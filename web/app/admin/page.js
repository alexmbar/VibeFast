export const metadata = { title: "Admin" }

// Placeholder: confirma que la validacion de rol funciona de punta a
// punta. El contenido real (metricas, usuarios, integraciones, costos)
// es el siguiente paso.
export default function AdminPage() {
  return (
    <div>
      <h1 className="text-2xl font-bold tracking-tight">Panel del dueño</h1>
      <p className="mt-2 text-sm text-base-content/70">
        Acceso concedido. Aquí van métricas de negocio, usuarios,
        integraciones y costos.
      </p>
    </div>
  )
}
